import { PipeFabricantePipe } from './pipe-fabricante.pipe';

describe('PipeFabricantePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFabricantePipe();
    expect(pipe).toBeTruthy();
  });
});
